import 'package:flutter/material.dart';
import 'speech_practice_screen.dart';

/// 别名入口：满足产出物路径约定
class SpeechEvaluateScreen extends StatelessWidget {
  const SpeechEvaluateScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const SpeechPracticeScreen();
  }
}